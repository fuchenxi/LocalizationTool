# Toast 提示功能

## 🎯 功能说明

双击表格单元格复制内容时，会在窗口底部显示优雅的 **Toast 提示**。

---

## ✨ Toast 设计

### 视觉效果
```
┌─────────────────────────┐
│  ✅ 已复制: action_cancel  │  ← Toast 提示框
└─────────────────────────┘
```

### 样式特点
- 🎨 **半透明深色背景**：`rgba(50, 50, 50, 200)`
- ⚪ **白色文字**：清晰易读
- 🔘 **圆角设计**：8px 圆角
- 📏 **合适的内边距**：10px 上下，20px 左右
- 📝 **字号**：13px，font-weight: 500

### 动画效果
```
淡入 (200ms)
    ↓
停留 (1300ms)
    ↓
淡出 (300ms)
    ↓
消失
```

总时长：**1.5 秒**

---

## 📍 位置

Toast 显示在**窗口底部居中**，向上偏移 60px：

```
┌─────────────────────────────────┐
│                                 │
│        主窗口内容               │
│                                 │
│                                 │
│         ┌─────────────┐         │
│         │ Toast 提示  │ ← 60px  │
│         └─────────────┘         │
└─────────────────────────────────┘
```

---

## 🎬 使用场景

### 1. 双击 Key
```
双击: action_cancel
    ↓
显示: ✅ 已复制: action_cancel
```

### 2. 双击 Value
```
双击: This is a very long translation text that needs to be truncated
    ↓
显示: ✅ 已复制: This is a very long transl...
```

### 3. 双击行号
```
双击: 935
    ↓
显示: ✅ 已复制: 935
```

---

## 💡 设计优势

### vs 日志提示
| 对比项 | 日志提示 | Toast 提示 |
|--------|----------|------------|
| **可见性** | 需要滚动查看 | 就地显示 |
| **干扰性** | 会占用日志空间 | 不干扰日志 |
| **视觉效果** | 普通文字 | 优雅动画 |
| **用户体验** | 一般 | ⭐⭐⭐⭐⭐ |

### vs 对话框
| 对比项 | 对话框 | Toast 提示 |
|--------|--------|------------|
| **干扰性** | 需要点击关闭 | 自动消失 |
| **操作流畅性** | 中断操作 | 不中断 |
| **视觉负担** | 重 | 轻 |
| **用户体验** | 一般 | ⭐⭐⭐⭐⭐ |

---

## 🔧 技术实现

### 核心代码
```python
class Toast(QLabel):
    def __init__(self, parent=None):
        super().__init__(parent)
        # 设置为工具提示类型的窗口
        self.setWindowFlags(Qt.WindowType.ToolTip | 
                           Qt.WindowType.FramelessWindowHint)
        # 半透明背景
        self.setAttribute(Qt.WidgetAttribute.WA_TranslucentBackground)
        
    @staticmethod
    def show_toast(parent, message: str, duration: int = 2000):
        toast = Toast(parent)
        toast.setText(message)
        # 计算位置
        # 显示
        toast.show()
        # 淡入淡出动画
        toast.fade_in()
        QTimer.singleShot(duration - 300, toast.fade_out)
        QTimer.singleShot(duration, toast.close)
```

### 动画实现
```python
def fade_in(self):
    self.animation = QPropertyAnimation(self.opacity_effect, b"opacity")
    self.animation.setDuration(200)
    self.animation.setStartValue(0)
    self.animation.setEndValue(1)
    self.animation.start()

def fade_out(self):
    self.animation = QPropertyAnimation(self.opacity_effect, b"opacity")
    self.animation.setDuration(300)
    self.animation.setStartValue(1)
    self.animation.setEndValue(0)
    self.animation.start()
```

### 使用方式
```python
# 显示 Toast
Toast.show_toast(self.window(), "✅ 已复制: action_cancel", 1500)
```

---

## 📊 效果对比

### 之前（日志提示）
```
[扫描日志区域]
正在扫描 en 语言...
✓ en: 无重复项
正在扫描 de 语言...
⚠ de: 发现 2 个重复项
📋 已复制 Key: action_cancel      ← 混在日志里
📋 已复制 Value: Cancel
📋 已复制 行号: 12
...（滚动才能看到）
```

### 现在（Toast 提示）
```
[扫描日志区域]
正在扫描 en 语言...
✓ en: 无重复项
正在扫描 de 语言...
⚠ de: 发现 2 个重复项
（日志保持干净）

[窗口底部]
┌───────────────────────────┐
│ ✅ 已复制: action_cancel  │ ← Toast 优雅显示
└───────────────────────────┘
（1.5秒后自动消失）
```

---

## 🎨 样式细节

### 半透明效果
```css
background-color: rgba(50, 50, 50, 200);
透明度: 200/255 ≈ 78%
```

### 圆角设计
```css
border-radius: 8px;
```

### 文字样式
```css
color: white;
font-size: 13px;
font-weight: 500;
padding: 10px 20px;
```

### 动画曲线
```python
QEasingCurve.Type.InOutQuad  # 先慢后快再慢的平滑曲线
```

---

## 🎯 用户体验提升

### Before (之前)
1. 双击复制
2. 滚动日志查看是否复制成功
3. 日志被新消息刷掉
4. 不确定是否复制成功

### After (现在)
1. 双击复制
2. Toast 立即显示 ✅
3. 确认复制成功
4. 1.5秒后自动消失
5. **完美！** 🎉

---

## 💡 设计理念

### 非侵入式
- 不遮挡主要内容
- 不中断用户操作
- 自动消失，无需关闭

### 即时反馈
- 立即显示，无延迟
- 淡入淡出，自然流畅
- 清晰的成功提示

### 视觉协调
- 与整体 UI 风格一致
- 不过于显眼，也不过于隐蔽
- 恰到好处的存在感

---

## 📝 文字截取

### 短文本
```
复制: Cancel
显示: ✅ 已复制: Cancel
```

### 长文本（超过 30 字符）
```
复制: This is a very long translation text that needs to be truncated for display
显示: ✅ 已复制: This is a very long transl...
```

---

**优雅、简洁、高效的提示方式！** ✨

